this is an asdf compatible version of cl-xml, repackaged for github.

it adds a dependency for de.setf.atn-parser, which supercedes the internal code.

the most recent usage is with sbcl in version 1.0.46

best regards, from berlin,

